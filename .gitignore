# secrets
cave-secret.json
*.token
.env
config/local.yaml

# data (lives outside the repo, junctioned in) -- anchored to repo root so it
# doesn't also match viewer/public/data (the small exported viewer assets, which
# ARE tracked)
/data/
*.feather
*.parquet

# python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# node / viewer
viewer/node_modules/
viewer/dist/

# logs, checkpoints, replays
/runs/
/checkpoints/
# raw match recordings live in C:\flyseek-data; viewer/public/replays (small exports) IS tracked
/replays/
*.log

# OS
Thumbs.db
.DS_Store

# third-party reference screenshots (other creators' content) -- local only, never pushed
Untitled_Message/

# Large replay binaries (Phase 6): kept locally, not committed
/viewer/public/replays/trained_*/
/viewer/public/replays/showcase*/
